node_modules
dist
.DS_Store
*.local

# Capacitor / Android 构建产物（android/ 源码保留，构建输出忽略）
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/capacitor-cordova-android-plugins/build/

# APK 构建产物
*.apk

# 用户原始导入数据（flomo 导出文本等）
scripts/flomo-export-*.txt

# WorkBuddy 本地记忆
.workbuddy/
.npm-cache/

# 食谱图片原始/压缩缓存（search-results.json 保留，可续跑补图）
scripts/recipe-images/cache/
